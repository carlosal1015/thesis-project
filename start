#!/usr/bin/env bash

function show() {
  echo "Checking version ... done!"
  lualatex -v
  arara -V
}

function run() {
  pushd src
  arara main.beamer main.report >/dev/null 2>&1
  popd
}

function export() {
  [ -d pdf ] || mkdir -p pdf
  mv src/main.*.pdf pdf
}

# show
run
export
